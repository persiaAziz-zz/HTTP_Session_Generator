## HTTP_Session_Generator
generates https sessions. Each session consists of multiple transactions. The session is written into JSON format
# Usage:
python Http_Txn_Generator.py --number 20 --dir ~/GenData/
